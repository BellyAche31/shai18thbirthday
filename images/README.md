# Images

Drop real photos here using the exact filenames referenced in `src/config.ts`
(`galleryImages`), e.g. `photo-01.jpg` through `photo-12.jpg`, plus `hero.jpg`
if you want to swap the hero background.

Until real files exist, the site automatically renders elegant generated
placeholders — nothing breaks and no broken-image icons appear.
